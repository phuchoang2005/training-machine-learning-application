---
title: "ADR-008: Real-Time Logs and Progress"
tags: [adr, websocket, realtime, streaming]
status: Accepted
aliases: [ADR-008]
---

# ADR-008: Real-Time Logs and Progress

**Status:** Accepted

**Decision:** Use WebSocket for MVP log, progress, and job status streaming, with REST polling as fallback for reconnect or degraded network scenarios.

**Version Target:** Standard browser WebSocket support with Spring Boot 4.0.x WebSocket support.

**Rationale:** The website needs a persistent real-time channel for training logs, progress updates, job status changes, and future interactive controls. WebSocket is well supported by React clients and Spring Boot backends and gives the platform a single channel for bidirectional real-time features.

**Consequences:**
- The backend must authenticate the WebSocket handshake and enforce project ownership for every subscribed job stream.
- The frontend must handle reconnect, duplicate event protection, and fallback polling when WebSocket is unavailable.

---

## Related
- [[adr-index]] — All ADRs
- [[ADR-015]] — Observer pattern (`JobStreamWebSocketHandler`) implements this
- [[ADR-011]] — Redux state updated by WebSocket events
- [[log-streaming-architecture-diagram]] — Log streaming architecture
- [[progress-event-flow-diagram]] — Progress event parsing flow
- [[realtime-state-flow]] — Frontend reconnect and state flow
- [[non-functional-requirements]] — NFR-PERF-002, NFR-PERF-003, NFR-REL-004, NFR-REL-005
